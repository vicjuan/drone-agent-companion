package com.durendal.droneagent.companion.vision.opencv.android

/**
 * Android runtime module marker. Distribution, ABI packaging and initializer
 * are deliberately selected and verified in issue #14.
 */
object AndroidOpenCvModule
